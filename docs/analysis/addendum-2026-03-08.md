# Sprint 1 Addendum — Undocumented Bugs Found During Implementation

Date: 2026-03-08

## BUG-001: check_control non-TUI stub accepts `&mut ()` but ControlReceiver is `Receiver<ClassicCancelSignal>`

**Discovered during**: PASO 1-A (build verification after ARCH-SYNC-1 fix)

**Location**:
- `crates/halcon-cli/src/repl/agent/provider_client.rs:258` — `check_control` non-TUI stub expects `&mut ()`
- `crates/halcon-cli/src/repl/agent_types.rs:14-15` — `ControlReceiver` (non-TUI) is `tokio::sync::mpsc::Receiver<ClassicCancelSignal>`

**Root Cause**: `agent_types.rs` was updated to add `ClassicCancelSignal` and change the non-TUI `ControlReceiver` type alias to `Receiver<ClassicCancelSignal>` (for PASO 1-C Ctrl-C support), but the `check_control` non-TUI stub in `provider_client.rs` was not updated to match — it still accepts `&mut ()`.

**Impact**: Build fails with `error[E0308]: mismatched types` when building without the `tui` feature (i.e., `--no-default-features`).

**Fix**: Update `check_control` (non-TUI stub) to accept `&mut tokio::sync::mpsc::Receiver<ClassicCancelSignal>` and handle `ClassicCancelSignal::Cancel` → `ControlAction::Cancel`. Also update any call sites in `convergence_phase.rs` and `post_batch.rs` that pass `ctrl_rx` to `check_control`.

**Fixed in**: PASO 1-C (see commit `feat(repl): Ctrl-C graceful cancellation — resolves GAP-CTRLC`)

---

## BUG-002: ARCH-SYNC-1 logic was duplicated inline in convergence_phase.rs

**Discovered during**: PASO 1-A investigation

**Location**: `crates/halcon-cli/src/repl/agent/convergence_phase.rs:534-539` (pre-fix)

**Description**: The ARCH-SYNC-1 fix (governance_rescue_active computed before adjudicate) was partially implemented by duplicating the SynthesisGate suppression condition inline (`last_convergence_ratio < 0.15 && rounds < 3`) instead of calling `synthesis_gate::evaluate()`. This created a maintenance risk: if the threshold changed in `synthesis_gate.rs`, the inline copy in `convergence_phase.rs` would silently diverge.

**Fix**: Replace inline condition with a direct call to `synthesis_gate::evaluate(GovernanceRescue, &ctx)` and derive `governance_rescue_active = !verdict.allow`.

**Fixed in**: PASO 1-A

---

## BUG-003: workspace `[workspace.dependencies]` does not support `optional = true`

**Discovered during**: PASO 2-B (AWS Bedrock provider)

**Location**: `Cargo.toml` workspace [workspace.dependencies] section

**Description**: Attempted to declare `aws-sigv4` and `gcp_auth` with `optional = true` in `[workspace.dependencies]`. Cargo rejects this with `aws-sigv4 is optional, but workspace dependencies cannot be optional`. The `optional` flag may only appear in the consuming crate's `[dependencies]` section, not in the workspace declaration.

**Fix**: Declare `aws-sigv4` and `gcp_auth` without `optional = true` in `[workspace.dependencies]`, and use `optional = true` in `crates/halcon-providers/Cargo.toml`. Note: this means all crates in the workspace pull in the dependency regardless; the feature gate at the Rust code level (`#[cfg(feature = "bedrock")]`) still prevents unused code compilation.

**Fixed in**: PASO 2-B inline

---

## BUG-004: gcp-auth crate name is `gcp_auth` (underscore), not `gcp-auth` (hyphen)

**Discovered during**: PASO 2-B/2-C

**Location**: `Cargo.toml` / `crates/halcon-providers/Cargo.toml`

**Description**: The GCP ADC crate is published on crates.io as `gcp_auth` (underscores) not `gcp-auth` (hyphens). Cargo could not find `gcp-auth` in the index.

**Fix**: Use `gcp_auth = { package = "gcp_auth", version = "0.12" }` in workspace Cargo.toml and `gcp_auth = { workspace = true, optional = true }` in halcon-providers.

**Fixed in**: PASO 2-C inline

---

## BUG-005: ContentBlock::ToolResult has non-Option fields (is_error: bool, content: String)

**Discovered during**: PASO 2-A (CiSink implementation)

**Location**: `crates/halcon-core/src/types/model.rs:114-118`

**Description**: The `ContentBlock::ToolResult` variant has `content: String` and `is_error: bool` (not `Option`), contrary to what might be assumed from the Anthropic API documentation where `content` is optional. CiSink initially treated these as `Option`, causing a type mismatch.

**Fix**: Updated CiSink's `tool_output()` to match the actual types: `content: String` and `is_error: bool` (no `Option` wrapper).

**Fixed in**: PASO 2-A inline

---

## BUG-006: Migration count assertions hardcoded to N rather than MIGRATIONS.len()

**Discovered during**: PASO 3-A (adding daily_user_metrics migration M36)

**Location**: `crates/halcon-storage/src/migrations.rs` — tests `migrations_run_on_fresh_db` and `migrations_are_idempotent`

**Description**: Both tests used hardcoded numeric literals (`assert_eq!(version, 35)` and `assert_eq!(count, 35)`) to verify the migration version. When a new migration (M36) was added, both tests failed with "left: 36, right: 35". The tests serve a valid regression-detection purpose but their hardcoding makes them a maintenance trap: every new migration requires updating them.

**Fix**: Updated literals from 35 to 36 to match the new migration count. Future mitigation: derive the expected count from `MIGRATIONS.len()` to make the tests maintenance-free.

**Fixed in**: PASO 3-A inline

---

## Sprint 4 Bugs (2026-03-08)

### BUG-S4-001: Stale `pub use reward::RewardPipeline` re-export

**Discovered during**: PASO 4-A (workspace build check)

**Location**: `crates/halcon-cli/src/repl/metrics/mod.rs:17`

**Description**: `pub use reward::RewardPipeline` re-exported a type that was removed from `reward.rs`. This caused a `error[E0432]: unresolved import` build failure that was introduced in a previous sprint and not caught because the TUI feature was always enabled in normal builds. The `--no-default-features` build revealed it.

**Fix**: Removed the stale re-export line.

**Fixed in**: PASO 4-A (mailbox implementation session)

---

### BUG-S4-002: Migration count tests hardcoded to 36

**Discovered during**: PASO 4-A (adding mailbox migration M37) and PASO 4-C (adding scheduled_tasks migration M38)

**Location**: `crates/halcon-storage/src/migrations.rs:1279,1293`

**Description**: Same pattern as BUG-003 from Sprint 3-A — hardcoded migration count assertions failed when new migrations were added. Updated to 38 (two new migrations added in Sprint 4: M37 mailbox_messages, M38 scheduled_tasks).

**Fix**: Updated literals from 36 to 38.

**Fixed in**: PASO 4-C inline

---

## BUG-007: Synthesis-premature-strip — tools removed when coordination steps exist mid-plan

**Discovered during**: Forensic runtime diagnosis (tool execution failure investigation)
**Severity**: CRITICAL — causes tools_executed=0, dependency_cascade in all mixed plans

**Location**:
- Primary: `crates/halcon-cli/src/repl/agent/mod.rs:1594-1596` (synthesis guard condition)
- Secondary: `crates/halcon-cli/src/repl/orchestrator.rs:976` (zero-tool soft-fail not caught)

**Root Cause**: The synthesis guard used `all(s.tool_name.is_none())` over pending steps.
Coordination/analysis steps generated mid-plan also have `tool_name: None`, so the guard
falsely fired on mixed plans (`[bash, file_write, <analyze>, summarize]`), stripping bash
and file_write before they could execute. Downstream tasks then entered `dependency_cascade`
because the expected evidence (files written, bash output) never materialized.

**Fix Applied**:
- FIX-1 (`mod.rs`): changed condition to `any_pending_execution = steps.any(tool_name.is_some())`;
  guard only fires when NO execution steps remain pending.
- FIX-2 (`orchestrator.rs`): zero-tool sub-agent "success" now treated as soft-fail for
  cascade propagation (dependents skipped, operators warned).

**Fixed in**: 2026-03-08 (this session)

---

### BUG-S4-003: PlaybookPlanner not checked in retry AgentContext

**Discovered during**: PASO 4-D code review

**Location**: `crates/halcon-cli/src/repl/mod.rs:3287`

**Description**: The primary invocation path (line 2898) correctly checks `PlaybookPlanner.find_match()` first and falls back to `LlmPlanner`. However, the retry path (when `ContextRefreshNeeded` error triggers a re-invocation) unconditionally passed `llm_planner.as_ref()` without checking the playbook planner. This meant that repeated tasks (git commit, test run, code review) would use the deterministic fast playbook on first invocation but fall back to a slow LLM-planned path on retry.

**Fix**: Applied the same `find_match()` check to the retry `AgentContext` construction.

**Fixed in**: PASO 4-D
